#include<iostream>

int check_sqrt(long int a, long int b, long int n)
{
    long int avg = (a+b)/2;
}
int main()
{
    long int n,a=0,b;
    int jpt;
    b=n;
    while(true)
    {
        jpt = check_sqrt(a,b,n);
        if(jpt)
    }
}